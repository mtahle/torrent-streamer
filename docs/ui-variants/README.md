# UI variants (not served)

The app serves `public/index.html` as the only supported web UI entrypoint (at `/`).

The HTML files in this folder are older UI experiments/variants kept for reference and are **not** served by `server.js`.

